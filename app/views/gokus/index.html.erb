<p id="notice"><%= notice %></p>

<h1>Gokus</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Precio</th>
      <th>Descripcion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @gokus.each do |goku| %>
      <tr>
        <td><%= goku.nombre %></td>
        <td><%= goku.precio %></td>
        <td><%= goku.descripcion %></td>
        <td><%= link_to 'Show', goku %></td>
        <td><%= link_to 'Edit', edit_goku_path(goku) %></td>
        <td><%= link_to 'Destroy', goku, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Goku', new_goku_path %>
