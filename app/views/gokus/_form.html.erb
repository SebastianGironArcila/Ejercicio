<%= form_for(goku) do |f| %>
  <% if goku.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(goku.errors.count, "error") %> prohibited this goku from being saved:</h2>

      <ul>
      <% goku.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :precio %>
    <%= f.number_field :precio %>
  </div>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_area :descripcion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
